# Stakeframe — v0.1.0-beta.10

| Campo        | Valor                                                                                                                                    |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Versão       | `0.1.0-beta.10` — atente: `package.json` na commit-fonte ainda registra `0.1.0-beta.9`; a renomeação segue em PR de preparação (docs-only) |
| Tag          | `v0.1.0-beta.10` — criada retroativamente para rastreabilidade do estado em produção (autorização do proprietário em 25/09/2026 09:06 BRT) |
| Commit-fonte | `311a5e4bea64ac0084664bacce597f53b06d1875` (`main`)                                                                                       |
| Canal        | Beta fechado (pré-lançamento)                                                                                                            |
| Estado       | **Em produção** — imagens publicadas no GHCR e serviço `operations` implantado na VPS (25/09/2026)                                        |

> Esta release é registro retroativo do estado em produção. Diferente das
> preparações anteriores, o deploy ocorreu antes da criação da tag, pelo fluxo
> `Release candidate` → `Publish approved candidate`, autorizado pelo
> proprietário em 24/09/2026 22:20 BRT (janela STK-A3 D6/D7).

## Resumo

Trabalho operacional do Gate 0: orquestração do projeto assumida pelo
OpenClaude (STK-A1), checklist de rotação de credenciais (STK-A2), check de
expiração TLS no monitor e na API com ensaio completo de renovação/reemissão
TLS em produção (STK-A3) e runbook/evidência do ensaio sintético de
backup/alerta (STK-A4).

## Entregas incluídas (b50ea43..311a5e4)

- #196 STK-A1 — orquestração transferida para o OpenClaude (AGENTS.md).
- #197 STK-A2 — checklist de rotação de credenciais (docs).
- #198 STK-A3 2A — check de expiração TLS no monitor e na API.
- #199 STK-A3 2B — ensaio ACME de renovação/reemissão TLS (Pebble) + evidência.
- #200 STK-A4 — runbook do ensaio sintético de backup (D1–D5).
- #201 STK-A3 D6 — aprovação do candidate ARM64 de `cc50eef`.

## Publicação e deploy (evidência)

- Manifest aprovado: `infra/release/approved-arm64.json` na commit-fonte
  (sourceSha `cc50eef96ad…`, candidate run 36083483562, publish run
  36089875469); cinco imagens ARM64 publicadas no GHCR por digest.
- Implantação na VPS limitada ao serviço `operations` (pin
  `sha256:64ffdcfa…`, 25/09 00:29); `api`, `worker`, `migrate` e
  `web-production` permanecem nas imagens beta.8 (`fed9cea…`, builtAt
  2026-09-23T20:30:08Z). O rodapé da web exibindo `v0.1.0-beta.8` reflete
  corretamente o runtime.
- Deploy com rehearsal TLS real: pin/digest conferido, container healthy,
  scan sem erros e rehearsal `stk-deploy-e9e887d9` com 14 checks (serials
  distintas). PROVA A (CI) + PROVA B (produção).
- A main seguiu de `311a5e4` para `ebb4849` apenas com documentação (#202).

## Validação

- CI 5/5: candidate run 36083483562 (head `cc50eef`) e main runs 36088793371
  (`311a5e4`) e 36114281507 (`ebb4849`, sucesso no re-run; a 1ª tentativa
  falhou em flake E2E Playwright com diff docs-only).
- Ensaio de backup STK-A4: alerta do monitor às 04:46Z (01:46 BRT) e
  recuperação às 05:11Z (02:11 BRT), dentro dos orçamentos do runbook,
  confirmados por escrito pelo proprietário; backup real sem falhas; zero
  resíduo pós-ensaio.
- Nenhuma migração de banco foi adicionada.

## Operação e limitações

- Esta release não altera regras de cálculo, ledger, liquidação, integrações
  ou configuração da importação automática.
- Imagens de produção devem ser consumidas por digest, nunca por `latest`.
- Tags e GitHub Releases beta.1–beta.9 permanecem imutadas (a nota da beta.9
  é atualizada nesta mesma PR).
