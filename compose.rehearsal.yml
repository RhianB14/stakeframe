# Applied only by the disposable local rehearsal runner.
services:
  web:
    ports: !override ['127.0.0.1::8080', '127.0.0.1::8443']
    volumes:
      - ./infra/production/tls.rehearsal.conf:/etc/caddy/tls.conf:ro
